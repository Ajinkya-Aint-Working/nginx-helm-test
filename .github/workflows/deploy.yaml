name: Deploy ArgoCD Application

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Install ArgoCD CLI
        run: |
          curl -sSL -o argocd \
            https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          chmod +x argocd
          sudo mv argocd /usr/local/bin/

      - name: Configure ArgoCD Environment
        run: |
          echo "ARGOCD_SERVER=${{ secrets.ARGOCD_SERVER }}" >> $GITHUB_ENV
          echo "ARGOCD_AUTH_TOKEN=${{ secrets.ARGOCD_AUTH_TOKEN }}" >> $GITHUB_ENV
          echo "ARGOCD_OPTS=--grpc-web" >> $GITHUB_ENV

      - name: Create or Update Application
        run: |
          argocd app create nginx-test \
            --repo https://github.com/Ajinkya-Aint-Working/nginx-helm-test.git \
            --path nginx-test-chart \
            --revision main \
            --dest-server https://kubernetes.default.svc \
            --dest-namespace nginx-test \
            --sync-policy automated \
            --sync-option CreateNamespace=true \
          || true

      - name: Wait for Healthy Status
        run: argocd app wait nginx-test --health --timeout 300

      - name: Show Status
        run: argocd app get nginx-test
