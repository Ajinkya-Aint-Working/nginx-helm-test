apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-test.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "nginx-test.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
